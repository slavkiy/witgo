# Scenario: Inspect

Этот сценарий показывает inspection без instantiation.

Он открывает общий component из `examples/components/basic/component.wasm` и
печатает:

- imports;
- exports;
- structural signatures функций.

Запуск:

```sh
go run ./examples/scenarios/inspect
```
